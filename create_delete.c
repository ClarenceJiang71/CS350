# include <stdio.h>
#include <stdlib.h>

// Create a 2x3 int array and initializes it with 0s 
int **create(){
    int **x;
    x = malloc();
};

void fill (int **x){
    x[0][0]
};

void foo(int **add){
    free(add);
};

void delete(int **x){
    for (int i =0; i<2; i++){
        free(x[i]);
    }
    free(x);
};



